{
  "installCommand": "cd codedocgen-frontend && npm install",
  "buildCommand": "cd codedocgen-frontend && npm run build",
  "outputDirectory": "codedocgen-frontend/build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
